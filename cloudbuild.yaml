steps:
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: "bash"
    args:
      - -c
      - |
        gcloud app create --project=inventory-management-425209 --region=us-central
        gcloud app deploy app.yaml --quiet

